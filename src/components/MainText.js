import React from "react";

const MainText = ({ text }) => {
  return (
    <p className="p-4 m-4 mb-0 overflow-hidden text-center text-2xl h-fit">
      {text}
    </p>
  );
};

export default MainText;
